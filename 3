#include <stdio.h>

int main() {
    int a, b, c;

    printf("Введите три числа (a, b, c): ");
    scanf("%d %d %d", &a, &b, &c);

    int sign = (a > 0) + (b > 0) + (c > 0);
    sign = (sign == 3) - (a < 0) - (b < 0) - (c < 0);

    printf("%d\n", sign);
    
    return 0;
}
