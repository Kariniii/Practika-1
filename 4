#include <stdio.h>

int main() {
    int a;
    
    printf("Введите целое число: ");
    scanf("%d", &a);

    int PowerOfTwo = (a > 0) & (!(a & (a - 1)));

    printf("%d\n", PowerOfTwo);

    return 0;
}

